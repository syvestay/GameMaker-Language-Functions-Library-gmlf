[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gmlf"
version = "0.1b1"
description = "GameMaker-like functions for Python"
authors = [
    {name = "Syvestay"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pygame"
]
